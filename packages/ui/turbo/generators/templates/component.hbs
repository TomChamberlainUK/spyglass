import * as React from 'react';
import styles from '../styles/{{ pascalCase name }}.module.scss';

interface Props {
  children?: React.ReactNode;
}

export default function {{ pascalCase name }}({ children }: Props) {
  return (
    <div className={styles['container']}>
      <h1>{{ name }}</h1>
      {children}
    </div>
  );
};
